AUX_SOURCE_DIRECTORY(. DIR_SRCS)
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -g -std=c++17")
INCLUDE_DIRECTORIES(${SunShine_SOURCE_DIR}/lib)
LINK_DIRECTORIES(${SunShine_BINARY_DIR}/lib)
ADD_EXECUTABLE(TestJson ${DIR_SRCS})
TARGET_LINK_LIBRARIES(TestJson libjson.a)